import type { FindingSeed } from "./types";

export const missingFindings: FindingSeed[] = [
  {
    id: "U-MISS-1",
    code: "U-MISS-1",
    title: "SettingsIO cannot save — 'load/save' is load-only",
    category: "missing",
    severity: "major",
    origin: "prior-audit",
    verification: "confirmed",
    evidenceGrade: "V1",
    files: "src/core/SettingsIO.h",
    foundBy: "S3 G1 · S1 B1",
    symptom:
      "The header describes itself as the load/save serialization the GUI will use to persist per-file settings and round-trip state. There is no writer. No presets, no per-file persistence, no CLI↔GUI round-trip.",
    rootCause: "Writer never implemented. load_settings_file even swallows open failures and returns a default Settings.",
    evidence:
      "This pass: grep save_settings over working_code/gifscythe → no matches. Header comment line 1–3 still says 'Load/save'.",
    fix: "save_settings() as the exact inverse of load_settings() plus a round-trip unit test load(save(s)) == s.",
    phase: "P1",
    sortOrder: 200,
  },
  {
    id: "U-MISS-2",
    code: "U-MISS-2",
    title: "No validation layer — bad values vanish silently",
    category: "missing",
    severity: "major",
    origin: "prior-audit",
    verification: "reproduced",
    evidenceGrade: "V1",
    files: "src/core/GifsicleCommand.h · src/core/SettingsIO.h",
    foundBy: "S3 G2 · S1 C9",
    symptom:
      "Out-of-range settings are dropped without a trace. colors=999 emits no -k. disposal=9 emits nothing. In a GUI, a control that 'does nothing' is indistinguishable from a bug.",
    rootCause:
      "GifsicleCommand gates on ranges (color_count 2–256, disposal 0–7) and simply omits the flag. No Warning type, no CLI print, no GUI hint.",
    evidence:
      "This pass: conf with colors=999 produced a command with no -k. lossy=abc became --lossy=0 via U-B10 rather than a parse error.",
    fix: "gs::validate(const Settings&) → vector<Warning>{field,value,reason}. CLI prints them. GUI shows inline hints.",
    phase: "P1",
    sortOrder: 210,
  },
  {
    id: "U-MISS-3",
    code: "U-MISS-3",
    title: "No safe-execution layer / no shell quoting / no ~ expansion",
    category: "missing",
    severity: "major",
    origin: "prior-audit",
    verification: "confirmed",
    evidenceGrade: "V1",
    files: "src/cli/main.cpp:43–50 · GifsicleCommand.h toString()",
    foundBy: "S3 G3 · S1 B5/B6",
    symptom:
      "Execution uses a shell (U-B6). Display is unquoted. resolve_path() treats leading ~ as 'absolute' and returns it unchanged, so the engine receives a literal tilde.",
    rootCause: "resolve_path: `if (p.empty() || p[0]=='/' || p[0]=='~') return p;`",
    evidence: "cli/main.cpp:44. No tilde expansion, no $HOME. toString has no quote helper.",
    fix: "argv exec + shell_quote for display. Expand ~ to $HOME or reject with a clear error.",
    phase: "P0",
    sortOrder: 220,
  },
  {
    id: "U-MISS-4",
    code: "U-MISS-4",
    title: "GUI: no async execution, no progress, no cancel",
    category: "missing",
    severity: "major",
    origin: "prior-audit",
    verification: "confirmed",
    evidenceGrade: "V1",
    files: "src/qtui/MainWindow.cpp:122–138 · MainWindow.h",
    foundBy: "S3 G4 · S2 N15",
    symptom:
      "WORKLIST task 4 (progress, cancel) has no foundation. Run blocks the UI thread. runButton_ stays enabled because the event loop is frozen, then a timeout can lie (U-B5).",
    rootCause: "QProcess is a stack local inside runCommand(), not a member, so signals cannot outlive the call.",
    evidence: "MainWindow.h has no QProcess member (only an unused #include <QProcess>). runCommand constructs `QProcess proc(this)` on the stack.",
    fix: "Member QProcess, started/readyReadStandardError/finished, busy state disabling Run, Cancel → kill(). Per-file progress in batch mode.",
    phase: "P2",
    sortOrder: 230,
  },
  {
    id: "U-MISS-5",
    code: "U-MISS-5",
    title: "No shared engine discovery / startup health check",
    category: "missing",
    severity: "major",
    origin: "prior-audit",
    verification: "confirmed",
    evidenceGrade: "V1",
    files: "src/cli/main.cpp · src/qtui/MainWindow.cpp:26",
    foundBy: "S3 G5 · S1 B4",
    symptom:
      "CLI and GUI each invented their own locator. CLI is CWD-sniffed (U-B2). GUI only looks at applicationDirPath()/gifsicle — which is never where build.sh puts the engine (release/<ver>/).",
    rootCause: "No gs::EngineLocator in core. GUI line 26 is a one-liner with no fallback and no .exe suffix (see NEW-2).",
    evidence:
      "GUI: `enginePath_ = QCoreApplication::applicationDirPath() + \"/gifsicle\";` — no exists() check at startup, no release/ probe, no GS_ENGINE.",
    fix: "gs::EngineLocator in src/core (exe-dir → GS_ENGINE → PATH → release/<GS_VERSION>), used by both fronts. Show path+version in the GUI status bar.",
    phase: "P1",
    sortOrder: 240,
  },
  {
    id: "U-MISS-6",
    code: "U-MISS-6",
    title: "Tests never exercise the flows that actually fail",
    category: "missing",
    severity: "major",
    origin: "prior-audit",
    verification: "reproduced",
    evidenceGrade: "V1",
    files: "tests/test_gifsicle_command.cpp · scripts/test_engine.sh · build.sh · .github/workflows/build.yml",
    foundBy: "S3 G6 · S1 B7",
    symptom:
      "35 in-process argv assertions on happy paths. Nothing covers --run exit codes, CWD sensitivity, merge-vs-batch, empty output, malformed conf, spaces in paths, or SettingsIO round-trip. test_engine.sh is not called by build.sh or CI.",
    rootCause: "Unit tests were written against the command builder only. IMPROVEMENT_LOG's 'integration tests pass' has no integration test.",
    evidence:
      "This pass: 35 CHECK() calls + 1 macro (grep CHECK( = 36). test_engine.sh 5/5 pass when invoked by hand. grep test_engine in build.sh and build.yml → no matches. Test 4 is also tautological (NEW-15).",
    fix: "CI smoke: --run from ≥2 CWDs, malformed-conf, merge-vs-batch, spaces-in-paths, SettingsIO round-trip, test_engine.sh both jobs, artifact upload.",
    phase: "P2",
    sortOrder: 250,
  },
  {
    id: "U-MISS-7",
    code: "U-MISS-7",
    title: "Drag-and-drop: enabled but not implemented",
    category: "missing",
    severity: "minor",
    origin: "prior-audit",
    verification: "confirmed",
    evidenceGrade: "V1",
    files: "src/qtui/MainWindow.cpp:44",
    foundBy: "S2 N4 · S1 A5",
    symptom:
      "inputList_->setAcceptDrops(true) with no dragEnterEvent/dropEvent override. Dropping files from the OS does nothing (QListWidget default DnD is internal item moves).",
    rootCause: "Flag set as a TODO marker. WORKLIST task 4 still open.",
    evidence: "Line 44 is the only drop-related call. MainWindow does not override drop events. No custom QListWidget subclass.",
    fix: "Subclass QListWidget or event-filter; accept Qt::CopyAction; append mimeData()->urls() with dedupe.",
    phase: "P1",
    sortOrder: 260,
  },
  {
    id: "U-MISS-8",
    code: "U-MISS-8",
    title: "No queue management controls",
    category: "missing",
    severity: "minor",
    origin: "prior-audit",
    verification: "confirmed",
    evidenceGrade: "V1",
    files: "src/qtui/MainWindow.cpp left column",
    foundBy: "S2 N5",
    symptom: "No Remove-Selected, no Clear-All. Combined with U-M2 (replace-not-append), users restart the app to drop one file.",
    rootCause: "MVP scaffold only wired Add.",
    evidence: "Left column widgets: label, QListWidget, one 'Add GIF files…' button. No other queue actions.",
    fix: "Remove-Selected + Clear-All. Already on WORKLIST task 4.",
    phase: "P1",
    sortOrder: 270,
  },
  {
    id: "U-MISS-9",
    code: "U-MISS-9",
    title: "No mode selector — Batch/Explode/Auto unreachable in the GUI",
    category: "missing",
    severity: "minor",
    origin: "prior-audit",
    verification: "confirmed",
    evidenceGrade: "V1",
    files: "src/qtui/MainWindow.cpp:108–113 · src/core/GifsicleSettings.h:22–27",
    foundBy: "S2 N3",
    symptom:
      "Mode::Merge is hardcoded. Mode::Batch, Explode, Auto exist in the core layer and settings format but no widget exposes them. This is why U-B3 happens.",
    rootCause: "GUI MVP only exposed optimize + lossy + output.",
    evidence: "currentSettings(): `gs::Settings s; s.mode = gs::Mode::Merge;`. No QComboBox in the file.",
    fix: "QComboBox Auto/Merge/Batch/Explode. Required once U-B3 is fixed so the user can choose 'optimize each' vs 'merge into one'.",
    phase: "P0",
    sortOrder: 280,
  },
  {
    id: "U-MISS-10",
    code: "U-MISS-10",
    title: "Dither is bool-only; docs promise method selection",
    category: "missing",
    severity: "minor",
    origin: "prior-audit",
    verification: "confirmed",
    evidenceGrade: "V1",
    files: "src/core/GifsicleSettings.h:78 · GifsicleCommand.h:82 · FEASIBILITY_REVIEW.md mapping table",
    foundBy: "S2 N9",
    symptom:
      "FEASIBILITY_REVIEW mapping table says --dither=floyd-steinberg|ro64|none. Core models bool dither and emits bare -f (default Floyd–Steinberg).",
    rootCause: "MVP mapped the short option only. Committed engine: -f is a separate flag; --dither is Clp_ValString with Clp_Optional, so methods need the attached form.",
    evidence:
      "gifsicle.c options[]: `{ 0, 'f', DITHER_OPT, 0, Clp_Negate }` and `{ \"dither\", 0, DITHER_OPT, Clp_ValString, Clp_Negate | Clp_Optional }`. Code emitting -f is correct as far as it goes.",
    fix: "std::string dither_method (\"\" = off) emitting -f or --dither=<method>.",
    phase: "P3",
    sortOrder: 290,
  },
  {
    id: "U-MISS-11",
    code: "U-MISS-11",
    title: "No LICENSE / GPL compliance artifacts in the product tree",
    category: "missing",
    severity: "minor",
    origin: "prior-audit",
    verification: "confirmed",
    evidenceGrade: "V1",
    files: "repo root · scripts/package_portable.sh",
    foundBy: "S1",
    symptom:
      "FEASIBILITY_REVIEW §5 is explicit: distributing gifsicle (GPL v2-only) alongside a GPLv3 UI requires shipping gifsicle's license text + source offer. There is no LICENSE/COPYING at repo root. package_portable.sh ships VERSION.md + README.md only.",
    rootCause: "Compliance deferred. Engine COPYING exists only under reference_code/gifsicle/COPYING — the packager never copies it.",
    evidence:
      "This pass find LICENSE/COPYING at depth≤3: only the two gifsicle trees. package_portable.sh:13 copies VERSION.md README.md.",
    fix: "Add COPYING (GPLv2 for the engine) + attribution; copy them into the portable folder; keep gifsicle a separate process forever unless Eddie Kohler grants a v3 exception.",
    phase: "P1",
    sortOrder: 300,
  },
  {
    id: "U-MISS-12",
    code: "U-MISS-12",
    title: "Windows packaging path incomplete",
    category: "missing",
    severity: "minor",
    origin: "prior-audit",
    verification: "confirmed",
    evidenceGrade: "V1",
    files: "scripts/package_portable.sh · .github/workflows/build.yml",
    foundBy: "S3 M7 · WORKLIST 1–2",
    symptom:
      "No windeployqt automation, no Windows GUI build in CI, no artifact, no smoke test of the portable folder on a clean machine. This is the whole remaining path to 1.0.0.",
    rootCause: "Packaging scripts were written on Linux against the qmake output layout.",
    evidence: "grep windeployqt over the repo: no matches except docs. Windows job has no upload-artifact.",
    fix: "windeployqt inside package_portable.sh on Windows; CI asserts portable-folder contents; clean-machine smoke test before 1.0.0.",
    phase: "P1",
    sortOrder: 310,
  },
  {
    id: "U-MISS-13",
    code: "U-MISS-13",
    title: "Remaining GifsicleSettings controls unexposed in GUI",
    category: "missing",
    severity: "minor",
    origin: "prior-audit",
    verification: "confirmed",
    evidenceGrade: "V1",
    files: "src/qtui/MainWindow.cpp · src/core/GifsicleSettings.h",
    foundBy: "WORKLIST task 5",
    symptom:
      "The GUI exposes 2 of ~30 modeled controls (optimize + lossy; output is a path, not a gifsicle feature). FEASIBILITY_REVIEW §3(2) already contains the complete slider↔flag mapping table.",
    rootCause: "Planned retrofit, not an accident — listed for completeness so it does not get lost behind 'MVP done'.",
    evidence:
      "Settings has mode, info, interlace, flips, rotation, position, crop, delay, disposal, loopcount, unoptimize, threads, colors, dither, gamma, color_method, careful, resize_*, background, transparent, comments, explode_by_name. GUI binds optimize_level, lossy, output, inputs.",
    fix: "Mechanical widgets↔settings work on top of the now-honest core. Keep the live raw-CLI pane. After P0/P1, not before.",
    phase: "P3",
    sortOrder: 320,
  },
];
